// Write two binary functions add and mul, that take two
// numbers and return their sum and product

var add = function add(a,b) {
  return a + b;
};

var mul = function mul(c,d) {
  return c * d;
};

console.log(add(3,4)); //7
console.log(mul(3,4)); //12
